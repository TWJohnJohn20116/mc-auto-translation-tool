plugins {
    id "net.fabricmc.fabric-loom-remap" version "${loom_version}"
}

ext.fabricMinecraftVersion = minecraft_version
ext.fabricYarnMappings = yarn_mappings
ext.fabricLoaderVersion = loader_version
ext.fabricApiVersion = fabric_api_version

apply from: rootProject.file("platforms/fabric/1.21/shared.gradle")
